<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Parse_Key__c</fullName>
    <label>Parse Key</label>
    <type>Text</type>
    <description>The exact JSON key the prompt template emits and the wizard/commit reads (e.g. company, candidateName). This is also the draft field name the LWC round-trips, so it is the single wire contract across prompt, LWC, and Apex.</description>
    <length>80</length>
    <required>true</required>
    <unique>false</unique>
</CustomField>
